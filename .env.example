# ElevenLabs — https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Green API (WhatsApp) — https://console.green-api.com
GREEN_API_INSTANCE_ID=your_instance_id
GREEN_API_TOKEN=your_api_token_instance

# WhatsApp group chatId (e.g. 120363XXXXXXXXXX@g.us)
# Find it: Green API console → Chats → right-click group → Copy chatId
GROUP_CHAT_ID=120363XXXXXXXXXX@g.us

# Unkey — https://app.unkey.com → APIs → create an API → copy apiId
UNKEY_API_ID=api_XXXXXXXXXXXXXXXXXX

# Unkey root key — https://app.unkey.com → Settings → Root Keys → create with verify_key permission
UNKEY_ROOT_KEY=unkey_XXXXXXXXXXXXXXXXXX

# OpenAI — https://platform.openai.com/api-keys (used by generate-and-send-image endpoint)
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXX
